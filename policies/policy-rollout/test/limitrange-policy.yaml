apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: policy-limitrange-test
  labels:
    policy: limit-range-test
spec:
  remediationAction: enforce
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: policy-limitrange-test
        spec:
          severity: medium
          namespaceSelector:
            include: ['test-*']
          object-templates:
            - complianceType: musthave
              recreateOption: IfRequired
              objectDefinition:
                apiVersion: v1
                kind: LimitRange
                metadata:
                  name: default-limit-range
                spec:
                  limits:
                  - type: Container
                    default:
                      cpu: '{{ if eq (index .ManagedClusterLabels "environment") "dev"}}500m{{ else }}1000m{{ end }}'
                      memory: '{{ if eq (index .ManagedClusterLabels "environment") "dev"}}512Mi{{ else }}1024Mi{{ end }}'
                    defaultRequest:
                      cpu: 600m
                      memory: 512Mi                    
