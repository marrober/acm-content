apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: configmap-placement-test
  labels:
    policy: configmap-test
spec:
  tolerations:
    - key: cluster.open-cluster-management.io/unreachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
  clusterSets:
    - dev-clusters
  predicates:
  - requiredClusterSelector:
      labelSelector:
        matchExpressions:
          - key: environment
            operator: In
            values:
            - "dev"
        