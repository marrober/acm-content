apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployer-sa-placement-binding.yaml
- deployer-sa-policy.yaml
patches:
- patch: |-
    - op: add
      path: /metadata/namespace
      value: policies-dev
  target:
    labelSelector: "policy=deployer-sa"
