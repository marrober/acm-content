apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: 04-pipeline-sa
  labels:
    policy: pipeline-sa
spec:
  disabled: false
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: pipeline-sa
        spec:
          evaluationInterval:
            compliant: 60s
            noncompliant: 60s
          customMessage:
            compliant: "No pipeline service accounts are in use"
            noncompliant: "pipeline service accounts are in use"
          severity: medium
          object-templates-raw: |   
            {{- range $pod := (lookup "v1" "Pod" "" "" "").items }}
              {{- if and (eq .spec.serviceAccountName "pipeline") (hasPrefix "dev-" .metadata.namespace)  }}
              - complianceType: mustnothave 
                objectDefinition:
                  kind: Pod
                  apiVersion: v1
                  metadata:
                    name: {{ .metadata.name }}
                    namespace: {{ .metadata.namespace }}
                  spec:
                    serviceAccountName: pipeline
              {{- end }}  
            {{- end }}
          remediationAction: inform